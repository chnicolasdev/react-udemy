

const activo = true;

// let mensaje = '';

// if ( activo ) {
//   mensaje = 'Activo';
// } else {
//   mensaje = 'Inactivo';
// }

// const mensaje = ( activo ) ? 'Activo' : 'Inactivo';

const mensaje = activo && 'Activo'; // Solo muestra una condición, es solo un IF

console.log( mensaje );